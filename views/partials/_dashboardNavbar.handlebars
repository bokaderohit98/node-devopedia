<nav class="navbar navbar-transparent navbar-absolute">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
      <a class="navbar-brand" href="#">{{header}}</a>
    </div>
    <div class="collapse navbar-collapse">
      {{#if user.isAdmin}}
        <form class="navbar-form navbar-right" role="search" method="post" action="/admin/search">
      {{/if}}
      {{#if user.isStudent}}
        <form class="navbar-form navbar-right" role="search" method="post" action="/student/search">
      {{/if}}
      {{#if user.isMentor}}
        <form class="navbar-form navbar-right" role="search" method="post" action="/mentor/search">
      {{/if}}
      <div class="form-group  is-empty">
        <input type="text" class="form-control" placeholder="Search" name="query">
        <span class="material-input"></span>
      </div>
      <button type="submit" class="btn btn-white btn-round btn-just-icon"><i class="material-icons">search</i>
        <div class="ripple-container"></div>
      </button>
      </form>
    </div>
  </div>
</nav>